# Reference copies (not tracked)

This folder is gitignored. Drop local copies of files that live in the frontend repo here so Claude can read them as reference without committing them.

Suggested files:
- customer_interaction_module.reference.php  (from GHOST_LASER_FRONTEND/project/customer_interaction_module.php)
- customer-interactions.reference.php        (from GHOST_LASER_FRONTEND/api/customer-interactions.php)
- 20260902_create_customer_interactions_table.reference.sql

Nothing in this folder is ever committed, so the two repos cannot fight over these files on deploy.
